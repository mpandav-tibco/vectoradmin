# Copy to .env.local and fill in values if needed.
# The app stores connection settings in the browser (localStorage) —
# these env vars are only needed to pre-seed defaults at build time.

# Optional: OpenAI API key pre-filled in the RAG settings panel
VITE_OPENAI_API_KEY=
